# owner-signal-domain-criome — Agent Instructions

Read `~/primary/AGENTS.md`, then this file.

This repository is a pure owner Signal contract crate. It declares
owner-only Criome-domain registry and policy records. It contains no daemon,
storage, name-server runtime, provider clients, or secret bytes.

Provider API credentials and provider-specific mutation belong to
`owner-signal-cloud`, not here.
